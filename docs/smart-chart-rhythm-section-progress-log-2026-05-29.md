# Smart Chart Rhythm Section Progress Log

Status: implementation breadcrumb log
Date: 2026-05-29
Branch: `codex/rhythm-section-core-authoring`
Source of truth: `docs/smart-chart-sprint-source-of-truth.md`

## Purpose

This log preserves the checkpoint trail from the final Rhythm Section side-sprint/V4 integration pass. Use it when hunting regressions across Lead Sheet baseline work, profile-owned tool policies, V4 extraction, or the closeout audit.

## Timeline

- 11:15 PT: Continued from V4 normalization plus Lead Sheet key-signature checkpoint; selected the smallest stable next slice after auditing plan gaps.
- 11:18 PT: Selected the Lead Sheet pitched-note baseline scope: model/API plus renderer layout for pitched events, without a live pitch recognizer.
- 11:35 PT: Implemented Lead Sheet pitched-note baseline: model/API, layout rendering, V4 visual note anchors, live finalization path, and stem-down quarter protection. Focused SwiftPM ChartEditing plus LeadSheetLayout passed with `84` tests; simulator `RhythmicNotationQuantizerTests` passed with `76` tests.
- 11:39 PT: Verified Lead Sheet pitched-note baseline: full SwiftPM `360` tests, `36` skipped, `0` failures; grouped simulator `182` tests, `0` failures; `git diff --check` clean; simulator build/run and screenshot succeeded with the known headermap warning.
- 11:46 PT: Implemented and verified Lead Sheet mixed note/rest slice: `setLeadSheetRhythmMap` stores pitch events only on note-capable slots, V4 anchors feed mixed finalization, and layout preserves rest glyphs. Focused SwiftPM `86` tests, simulator quantizer `77` tests, grouped simulator `185` tests, full SwiftPM `362` tests, and build/run all passed.
- 11:52 PT: Added and verified Lead Sheet beamed-eighth pitch-anchor coverage. V4 beamed crops provide separate staff-position anchors for accepted eighth values. Focused simulator `1` test, simulator quantizer `78` tests, grouped simulator `186` tests, full SwiftPM `362` tests, and build/run all passed.
- 11:56 PT: Completed Lead Sheet pitch-coverage guard. `setLeadSheetRhythmMap` rejects partial, duplicate, or rest-slot pitch coverage so every note-capable slot needs one V4 anchor. ChartEditing `48` tests, grouped simulator `186` tests, full SwiftPM `362` tests, `git diff --check`, and build/run all passed.
- 11:59 PT: Completed profile-owned user-facing rhythm-note editing rule. Simple and Rhythm Section stay disabled; Lead Sheet keeps the existing edit surface. ChartEditing `48` tests, grouped simulator `186` tests, full SwiftPM `362` tests, and build/run all passed.
- 12:04 PT: Completed profile-owned freehand ink availability. Simple and Rhythm Section resolve freehand symbol scopes; Lead Sheet has no freehand page-ink scope until defined. Focused simulator `66` tests, grouped simulator `187` tests, full SwiftPM `362` tests, and build/run all passed.
- 12:09 PT: Completed profile-owned rhythmic-notation ink availability. Simple has no rhythmic-notation active ink/tab path; Rhythm Section and Lead Sheet keep it. ChartEditing `48` tests, focused simulator `67` tests, grouped simulator `188` tests, full SwiftPM `362` tests, and build/run all passed.
- 12:15 PT: Completed V4 raster/template core extraction. V4 raster input, crops, templates, phrase decision, render comparison, and note anchors moved into `RhythmicNotationRasterTemplateRecognizer.swift`; `RhythmicNotationQuantizer` remains the entrypoint and V3/visual/fallback bridge. Focused simulator `78` tests, grouped simulator `188` tests, full SwiftPM `362` tests, `git diff --check`, and build/run all passed.
- 12:22 PT: Completed shared rhythm recognition type extraction. Quantization errors, proposal safety, phrase/primitive hypotheses, decisions, and reasons moved into `RhythmicNotationRecognitionTypes.swift`; quantizer remains the orchestration entrypoint. Focused simulator `78` tests, grouped simulator `188` tests, full SwiftPM `362` tests, `git diff --check`, and build/run all passed.
- 12:25 PT: Completed `RhythmCandidate`/`CandidatePath` extraction into `RhythmicNotationRecognitionTypes.swift`. Focused simulator `78` tests, grouped simulator `188` tests, full SwiftPM `362` tests, `git diff --check`, and build/run all passed.
- 12:27 PT: Added Rhythm Section/V4 closeout audit documenting implemented core authoring, V3/V4 recognition, profile-owned tool policies, Lead Sheet baseline, verification snapshot, and definition-gated deferred systems.
- 12:28 PT: Completed source-of-truth drift cleanup: active/local-complete status text reconciled, outdated Lead Sheet pitched-note-deferred wording replaced with richer melody/ledger/named-pitch deferrals, and side-sprint closure routing clarified.
- 2026-06-01 12:10 PT: Completed live rhythm route cleanup after replaying saved Rhythm Section ink from the failing simulator pass. Removed the obsolete rhythm confirmation sheet/callback path, routed live and finalization decisions through `LeadSheetRhythmicNotationLiveDecisionPolicy`, kept all non-commit states local, and fixed two V4 evidence bugs: stems no longer count as their own upper flag mass, and right-leaning quarter noteheads are not reattached backward as dotted-rhythm dots.
- 2026-06-01 12:12 PT: Verified the cleaned route. Focused simulator replay/regression tests passed with `2` tests; grouped simulator `RhythmicNotationQuantizerTests` plus `LeadSheetInteractionModeStatePolicyTests` passed with `110` tests; full SwiftPM passed with `443` tests, `36` skipped; simulator build/run and screenshot capture passed with the known headermap warning only.
- 2026-06-01 12:55 PT: Fixed the follow-up four-slash Rhythm Section failure from the live simulator pass. The saved ink was being routed to the visual fallback, where the second slash could be claimed as an eighth rest and leave the measure underfilled at `7/8` units. V4 now owns isolated placeholder-slash crops directly, emits one `.slash` per isolated slash stroke even when crops group tightly, and the fallback eighth-rest branch explicitly refuses isolated placeholder-slash symbols.
- 2026-06-01 12:56 PT: Verified the V4 slash authority fix without adding handwriting fixtures, score retuning, OCR, or default diagnostics. Focused simulator replay/regression tests passed with `2` tests; grouped simulator `RhythmicNotationQuantizerTests` plus `LeadSheetInteractionModeStatePolicyTests` passed with `111` tests; full SwiftPM passed with `443` tests, `36` skipped; `git diff --check` passed; simulator build/run and screenshot capture passed with the known headermap warning only.
- 2026-06-01 13:14 PT: Added a clear stale-state for silent rhythm non-renders. Underfilled or otherwise quiet preserve-ink decisions still stay quiet while the user may be writing, but if unchanged ink survives the normal auto-render window without a commit or targeted unread crop, the canvas now shows the existing dashed feedback around the actual ink bounds rather than leaving the measure visually idle. The live replay harness now skips cleanly when `SMART_CHART_RHYTHM_STATE_LIVE=1` points at a simulator state with no pending raw rhythm ink.
- 2026-06-01 13:34 PT: Added native iPad viewport/orientation support for the upcoming Rhythm Section live pass. The generated app metadata now requires full-screen iPad presentation and declares portrait/landscape orientations; `EditorView` sizes the canvas from the actual viewport; Simple/Rhythm paper widths expand to native app bounds; Rhythm Section rows stretch their automatically packed measures across the usable staff row; Lead Sheet keeps the legacy capped paper until post-V1. Focused layout/config SwiftPM passed with `70` tests; full SwiftPM passed with `447` tests, `36` skipped; simulator build/run passed with the known headermap warning. Manual Simulator rotation was unavailable in the current desktop session because the orientation menu items were disabled, so visual landscape proof remains a live-pass checklist item.
- 2026-06-02 08:45 PT: Fixed the clear whole-note Rhythm Section non-render without opening a new live pass. The recognizer already read the strong hollow notehead as `.whole`, but the older whole-measure safety guard converted the proposal to manual review, which surfaced as unread feedback after the confirmation workflow was removed. V4 now promotes strong single-crop whole-note evidence to auto-apply while whole rests and tiny whole-like marks stay conservative.
- 2026-06-02 09:00 PT: Tightened the V1 creation/library contract without changing recognition. New Chart now uses `ChartLayoutStyle.v1NewChartOptions`, exposing only Simple Chord Sheet and Rhythm Section Sheet while preserving Lead Sheet as post-V1 compatibility. Library summaries now derive from layout setup policy, so Simple and Rhythm Section no longer leak key names. Added a V1-shaped Rhythm Section persistence proof covering rhythm maps with drawing data, preserved raw rhythm ink, chords, cue text, roadmap objects, below-measure freehand articulations, typography, and selected-chart state. Added a store-level editor-binding persistence proof that mutating an element inside `ChartLibraryStore.charts` writes the edited chart into the saved snapshot.
- 2026-06-02 09:28 PT: Audited the live simulator state after a report that rhythm recognition and chord persistence both looked broken. The selected Rhythm Section chart preserved measure-1 raw rhythmic ink with no committed `rhythmMap`; V4 replay read the ink as underfilled (`dottedQuarter`, `eighth`, `quarter`, `6/8` units in `4/4`), so the commit gate correctly refused to render. Saved unresolved rhythmic ink now draws a dashed red outline when it is no longer in the active writing canvas, making the preserved non-commit state visible after reopening or switching tools. The chord side exposed an over-broad local rejected-auto-render rule: a deleted one-candidate `C` stored `["C"]` and forced future confident `C` ink into confirmation. Rejected auto-render memory now only uses broad candidate-signature blocking for multi-candidate races; single-candidate deletes block only the exact ink digest.
- 2026-06-02 09:57 PT: Added the persistence regression gate requested after the top-band chord-lane fix. The gate now round-trips a mixed V1 library snapshot containing both Simple and Rhythm Section charts, selected-chart state, entitlements, pending page chord ink, committed chord source ink/signatures, committed rhythm maps with drawing data, unresolved raw rhythmic ink, typography, manual Simple row/width state, roadmap objects, cue text, and freehand symbols. The store editor-binding proof now verifies that mutating a chart through `ChartLibraryStore.charts`, the same shape used by the live editor binding, persists pending chord ink, committed rhythm ink/maps, unresolved rhythm ink, and chord source evidence together.
- 2026-06-02 10:25 PT: Fixed the live two-half-note Rhythm Section failure from the simulator pass. The saved raw ink persisted correctly, but V4 selected the shorter quarter-note candidate for each ambiguous hollow-stem crop, returned `underfilled` at `4/8` units, and never reached the aligned exact-fit `half, half` path. The underfilled branch now checks a writer-agnostic, render-aligned exact-fit promotion before preserving ink; the saved pass replays to `half, half` commit. No handwriting fixture, score retuning, OCR expansion, or default diagnostic stream was added.
- 2026-06-02 10:33 PT: Followed up on the compact whole-note live failure. The earlier whole-note guard only promoted larger hollow-head crops, so a normal small handwritten oval could stay in unread feedback even though the v1 vocabulary supports whole notes. Strong whole-note promotion now accepts compact hollow-head crops at the template-recognition scale, while the tiny low-information whole-like mark guard still refuses auto-apply.
- 2026-06-02 10:39 PT: Replaced the over-specific whole-note hollow-head requirement with the direct v1 rule: in the rhythm lane, a no-stem closed oval/circle above the tiny-mark threshold is a whole note. Literal closed-circle, compact oval, and regular whole-note simulator tests commit as `.whole`; tiny low-information marks still fail closed.

## Latest Verification

- Chord correction-memory focused SwiftPM: `8` tests, `0` failures.
- Focused simulator `LeadSheetInteractionModeStatePolicyTests`: `31` passed, `0` failed.
- Grouped simulator `RhythmicNotationQuantizerTests` and `LeadSheetInteractionModeStatePolicyTests`: `111` passed, `1` optional live replay skipped, `0` failed.
- Full SwiftPM: `443` tests, `36` skipped, `0` failures.
- Native viewport/layout focused SwiftPM: `70` tests, `0` failures.
- Native viewport full SwiftPM: `447` tests, `36` skipped, `0` failures.
- Whole-note focused simulator `RhythmicNotationQuantizerTests`: `80` passed, `1` optional replay skipped, `0` failed.
- Whole-note grouped simulator `RhythmicNotationQuantizerTests`, `LeadSheetInteractionModeStatePolicyTests`, and `ChartEditingTests`: `200` passed, `1` optional replay skipped, `0` failed.
- Whole-note full SwiftPM: `450` tests, `36` skipped, `0` failures.
- Half-note ambiguity focused simulator replay/regression: `2` tests, `0` failures.
- Half-note ambiguity simulator `RhythmicNotationQuantizerTests`: `81` passed, `1` optional replay skipped, `0` failed.
- Compact whole-note focused simulator: `3` tests, `0` failures.
- Compact whole-note simulator `RhythmicNotationQuantizerTests`: `82` passed, `1` optional replay skipped, `0` failed.
- Direct circle whole-note focused simulator: `4` tests, `0` failures.
- Direct circle whole-note simulator `RhythmicNotationQuantizerTests`: `83` passed, `1` optional replay skipped, `0` failed.
- V1 creation/library focused SwiftPM: `18` tests, `0` failures.
- V1 creation/library focused SwiftPM after editor-binding proof: `14` tests, `0` failures.
- Persistence regression gate focused SwiftPM: `20` tests, `0` failures.
- Persistence regression gate full SwiftPM: `458` tests, `36` skipped, `0` failures.
- V1 creation/library full SwiftPM: `455` tests, `36` skipped, `0` failures.
- V1 creation/library focused simulator: `13` tests, `0` failures.
- Simulator build/run and screenshot: passed with the known headermap warning; post-audit screenshot confirmed the unresolved raw rhythm ink outline.
- `git diff --check`: passed after this doc update.
